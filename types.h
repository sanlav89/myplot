#pragma once

#include <memory>
#include <vector>

using spectrum_t = std::vector<int>;
using proba_t = float;
using probas_t = std::vector<proba_t>;
using data_t = int;
using nuclideid_t = int;
using nuclides_t = int;
using activities_t = int;
using enpoly_t = std::vector<double>;

const int SpectrumSize = 1024;
